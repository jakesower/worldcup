import { addIndex, chain, map, mapObjIndexed, merge, mergeAll, range, reduce } from 'ramda';
import { pipeThru } from './utils';
import results from '../data/2018-results.json';

// Given the results of a group, what's the outcome.
function groupRankings(matches) {

}
